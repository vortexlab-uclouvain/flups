# build image
docker build -t immc/flups-valid:v0.4 .
#v0.3 has valgrind but not metis

# push image to docker-hub
docker login
docker push immc/flups-valid:v0.4

# local test:
docker run -it --rm immc/flups-valid:v0.4
